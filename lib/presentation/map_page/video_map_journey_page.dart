import 'package:flutter/material.dart';

class VideoJourneyPathMapWidget extends StatelessWidget {
  const VideoJourneyPathMapWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
